import styled from 'styled-components/native';

const Subtitle = styled.Text`
  font-family:Inter;
  font-size: 24px;
  line-height: 36px;
  font-style: normal;
  font-weight: 600;
`;

export default Subtitle;
